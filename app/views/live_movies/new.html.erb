<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New live movie</h1>

  <%= render "form", live_movie: @live_movie %>

  <%= link_to 'Back to live movies', live_movies_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
